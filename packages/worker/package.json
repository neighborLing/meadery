{
  "name": "worker",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "typescript": "^4.8.4",
    "worktop.build": "^0.0.5",
    "wrangler": "2.1.10"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "worktop build ./src/index.ts"
  },
  "dependencies": {
    "faunadb": "^4.7.0",
    "worktop": "0.7"
  }
}
